# Docker Profile - Container Environment Configuration

# Server Configuration - Use port 8080 internally (mapped to 8090 externally)
server.port=8080

# Reduced logging for Docker
logging.level.com.payment.gateway=INFO
logging.level.org.springframework.web=WARN

# Mock Gateway Configuration - Production-like settings
mock.payment.success-rate=0.9
mock.payment.processing-delay=1000