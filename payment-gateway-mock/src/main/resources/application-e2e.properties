# E2E specific settings for Payment Gateway Mock

server.port=8080

# This mock likely doesn't use DB, Flyway, or Kafka.
# If it does, configure them appropriately for E2E.
# spring.datasource.url=
# spring.flyway.enabled=false
# spring.jpa.hibernate.ddl-auto=none
# spring.kafka.bootstrap-servers=

# E2E specific mock behavior
# app.mock.force-failure-for-card=ends_with_1111
# app.mock.response-delay-ms=100

# Logging Configuration for E2E
logging.level.root=INFO
logging.level.com.insurance.mock=DEBUG
logging.level.org.springframework=WARN